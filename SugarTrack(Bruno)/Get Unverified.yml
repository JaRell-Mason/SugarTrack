info:
  name: Get Unverified
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/users/unverified
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
