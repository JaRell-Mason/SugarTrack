info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/users
  body:
    type: json
    data: |-
      {
        "email": "test@parentthesequel.com",
        "password": "TestythePass"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
