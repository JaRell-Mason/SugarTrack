info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/users
  body:
    type: json
    data: |-
      {
        "email": "test@parentthefivequel.com",
        "password": "12345678",
        "displayName": "Totally Real Person"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
