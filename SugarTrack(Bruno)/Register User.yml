info:
  name: Register User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/users
  body:
    type: json
    data: |-
      {
        "email": "test@parentthethreequel.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
